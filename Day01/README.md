# Day 1 — Java Basics + Two Sum

## JVM vs JDK vs JRE
- JDK  → install this (compiler + tools + JRE)
- JRE  → only runs Java
- JVM  → runs bytecode, inside JRE

## Data Types
- int, long     → whole numbers
- double, float → decimals
- String        → text
- char          → single character
- boolean       → true/false

## Print
- System.out.println() → with new line
- System.out.print()   → without new line

## Programs Written
- areaOfCircle.java
- bmiCalculator.java
- celsiusToFahrenheit.java
- OddEven.java
- simpleInterest.java

## Two Sum (LC #1)
- Brute Force → O(n²) two loops
- HashMap     → O(n) one loop 
- complement  = target - current number
- store number as key, index as value

## Git Commands
- git init       → start tracking
- git add .      → stage files
- git commit     → save snapshot
- git push       → upload to GitHub